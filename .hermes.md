# Backup-Tool — agent workspace

**Project:** `~/Projects/Backup-Tool` (NormG/Backup-Tool)  
**Hermes cwd:** pinned here via `terminal.cwd` in `~/.hermes/config.yaml`

## Current mission (2026-08-07)

Multi-agent Mastodon publish investigation — Hermes leads, Cursor executes, Codex supports.

### Mastodon services (user systemd)

| Unit | Role | Status |
|------|------|--------|
| `caduceus-gateway.service` | DM notify API :8787 | running |
| `caduceus-elf-post.timer` | Hourly **elf trail** public posts (@caduceus22) | active — **queue empty** (7/7 posted Aug 7) |
| `kapyong-post.timer` | Hourly **Kapyong art** single image | **was broken** — `network-online.target not found` in user session; fixed 2026-08-07 |

### Root cause (Cursor vs Hermes confusion)

- **Hermes elf posts did work** — see `~/.config/caduceus/elf_post_queue.json` (all 7 URLs on mastodon.social).
- Timer now logs `queue complete` — not a failure; need to **refill queue** or add images for more posts.
- **Kapyong (Cursor setup)** timer failed silently — bad unit (`Requires=network-online.target`, `User=norm` in user unit). Manual `post_kapyong_image.py` could still work.

### Shared credentials (never commit)

- Token: `~/.config/caduceus/mastodon.token`
- Config: `~/.config/caduceus/config.toml`
- Gateway: `curl -s http://127.0.0.1:8787/health`
- Skill: `mastodon-caduceus` (Cursor, Hermes, Codex)

### Agent roles

- **Hermes:** orchestrate via **tri-agent bus** — read `~/.hermes_and_cursor_drop_box/bus/BOARD.md` first; assign tasks; do not let Cursor/Codex solo-run
- **Cursor:** IDE executor — ACK assigned BOARD tasks only; wait for Hermes PLAN when Norm says "lead the group"
- **Codex:** CLI executor — ACK assigned BOARD tasks only

**Bus protocol:** `~/.hermes_and_cursor_drop_box/20260807_SYSTEM_PROTOCOL_tri-agent-bus.md`  
**Live board:** `~/.hermes_and_cursor_drop_box/bus/BOARD.md`

## Backup-Tool trail (this machine)

| | Path |
|---|------|
| Config | `~/.config/backup-tool/config.toml` |
| Backs up | `/home/norm` → **`/mnt/home_backups/verona1`** |
| Drive | `/dev/sda1` automounted at `/mnt/home_backups` |
| Snapshots | `full-*`, `inc-*`, `latest` → e.g. `inc-2026-08-07_020457` |
| Drop-box archives | `/mnt/home_backups/verona1/.drop-box-archive/compressed/` |


```bash
cd ~/Projects/Backup-Tool
systemctl --user list-timers '*post*' '*caduceus*'
journalctl --user -u kapyong-post.service -n 20 --no-pager
journalctl --user -u caduceus-elf-post.service -n 10 --no-pager
cd ~/Projects/caduceus-gateway && .venv/bin/python scripts/post_next_elf.py --dry-run
curl -s http://127.0.0.1:8787/health
```
